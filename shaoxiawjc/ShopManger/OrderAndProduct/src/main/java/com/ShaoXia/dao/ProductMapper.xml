<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ShaoXia.dao.ProductMapper">
    <!--查询全部的商品-->
    <select id="selectAllProduct" resultType="product">
        select * from product
    </select>

    <!--根据id查询指定商品-->
    <select id="selectProductById" parameterType="_int">
        select * from product where product_id = #{id}
    </select>

    <!--增加商品-->
    <insert id="insertProduct" parameterType="product">
        insert into product (product_id,product_name,product_price,create_time)
        values (#{productId},#{productName},#{productPrice},#{create_time})
    </insert>

    <!--删除商品，同时由于外键，关联表里相关的数据会被一并删除-->
    <delete id="deleteProduct" parameterType="_int">
        delete from product where product_id = #{id}
    </delete>

    <!--更新商品-->
    <update id="updateProduct" parameterType="map">
        update product
        <set>
            <if test="product_name != null">
                product_name = #{product_name}
            </if>
            <if test="product_price != null">
                , product_price = #{product_price}
            </if>
            <if test="update_time != null">
                , update_time = #{update_time}
            </if>
        </set>
        where product_id = #{product_id}
    </update>
</mapper>
